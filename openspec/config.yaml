schema: intent-driven

#   context: |

rules:
  proposal:
    - Must use grill-me skill before writing proposal AND Must use adversarial-authoring skill after writing proposal
  design:
    - Must use glossary skill and use simple technical language while writing design and glossary
  adr:
    - Must use architectural-decision-records skill
  specs:
    - Must use gherkin-authoring skill
  tasks:
    - Must read docs/TASK_INSTRUCTIONS.md BEFORE exploring the codebase and planning any tasks.
