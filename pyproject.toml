[project]
name = "imgdb"
version = "0.1.0"
description = "Image query database"
readme = "README.md"
requires-python = ">=3.12"

[tool.uv.workspace]
members = [
    "backend",
    "cli",
]

[dependency-groups]
test = [
    "pytest>=8.0",
    "httpx2>=0.27",
    "pillow>=12.2.0",
]
