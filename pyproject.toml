[project]
name = "imgdb"
version = "0.1.0"
description = "Image query database"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "transformers>=5.12.1",
]

[tool.uv.workspace]
members = [
    "backend",
]
